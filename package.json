{
  "name": "popup-direct",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "prestart": "node build.js",
    "start": "node server.js",
    "deploy": "bash deploy.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
